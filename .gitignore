# bidon-solana
target/
.anchor/
test-ledger/
node_modules/
.env
.env.*
*.local
dist/

# установленные сторонние агент-скилы (ZK Compression и пр.) — не в репо
.agents/

# ключ relayer для e2e (devnet-only, не в репо)
.relayer.json
# отдельный ключ кипера (#9: газ+подпись стора, НЕ Kora-релеер; devnet-only, не в репо)
.keeper.json

# локальные скретч-скрипты и логи (не в репо)
disc*.sh
build.sh
*.log
devnet-check.sh
devnet-setup.sh

# не для ПУБЛИЧНОГО репо — хранится локально (приватная стратегия, бинари, legacy, ассеты)
/pro-tasks/
/bidon/
/clip/
/.pitchbuild/
/.claude/
/.mcp.json
/skills-lock.json
/PLAN.md
/SOLANA_PLAN.md
/PRODUCT_AND_FRONTEND.md
/bidon-zk/kora/kora-bin
bidon-banner*
